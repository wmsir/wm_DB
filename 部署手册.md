# wmDB 自动化部署手册

本文档详细介绍了如何将 wmDB 前后端项目自动化部署到指定的远程服务器。

## 目标服务器信息

*   **服务器 IP**: `<SERVER_IP>`
*   **SSH 端口**: 22 (默认)
*   **登录账号**: `<SERVER_USER>`
*   **登录密码**: `<SERVER_PASS>`

## 数据库配置信息

*   **JDBC URL**: `<DB_URL>`
*   **用户名**: `<DB_USER>`
*   **密码**: `<DB_PASS>`

## 系统默认测试账号与登录凭证

部署完成后，可直接在 Web 登录页面使用以下测试凭证进行验证：

| 登录模式 | 登录账号 / 手机号 | 密码 / 验证码 | 角色权限 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **用户名密码登录** | `testadmin1` | `123456` | ADMIN (系统管理员) | 拥有平台最高权限 |
| **用户名密码登录** | `testadmin2` | `123456` | ADMIN (系统管理员) | 备用管理员账号 |
| **身份证号登录** | `310101199001011234` | `123456` | ADMIN (系统管理员) | 实名制安全登录 |
| **身份证号登录** | `11010519880808567X` | `123456` | ADMIN (系统管理员) | 实名制安全登录 |
| **手机验证码登录** | `13800000001` (或任意11位手机号) | `123456` (通用测试验证码) | DEV (开发工程师) | 点击“获取验证码”后输入即可登录 |

---

## Swagger API 管理与在线调试地址

*   **Swagger UI 控制台**: `http://<SERVER_IP>:8080/swagger-ui/index.html`
*   **OpenAPI 3.0 元数据**: `http://<SERVER_IP>:8080/v3/api-docs`

支持在 Swagger UI 中点击 `Authorize` 输入 JWT Token 进行权限接口调试。

---

## 部署前提条件

1.  **本地构建环境**: 需要安装 Node.js (建议 v20+), Maven, 和 JDK 17。
2.  **网络互通**: 本地机器需要能够通过 SSH 访问目标服务器 `<SERVER_IP>`。
3.  **依赖工具**: 本地机器需要安装 `sshpass` 工具以实现免交互传输和执行。

## 自动化部署步骤

项目根目录下已提供 `deploy.sh` 脚本，可实现一键构建与部署。

### 1. 脚本概览 (`deploy.sh`)

脚本主要完成以下工作：
1.  **后端打包**: 使用 `mvn clean package -DskipTests` 将 Spring Boot 后端项目打包为 `.jar` 文件。
2.  **前端打包**: 使用 `npm install` 和 `npm run build` 构建 Vue 3 前端，生成 `dist` 目录。
3.  **目录创建**: 通过 SSH 连接到远程服务器，并在 `/opt/wmdb/backend` 和 `/opt/wmdb/frontend` 创建部署目录。
4.  **文件传输**: 使用 `scp` 将后端的 `.jar` 包和前端压缩后的 `dist` 文件传输到服务器对应目录。
5.  **启动后端服务**: 在远程服务器上，解压前端资源，杀掉旧的后端进程，并使用 `nohup java -jar` 结合预设的数据库配置启动新的后端服务。

### 2. 执行部署

1.  确保本地已赋予脚本执行权限：
    ```bash
    chmod +x deploy.sh
    ```
2.  在终端中设置必要的环境变量并执行部署脚本：
    ```bash
    export SERVER_IP="<SERVER_IP>"
    export SERVER_PASS="<SERVER_PASS>"
    export DB_URL="<DB_URL>"
    export DB_USER="<DB_USER>"
    export DB_PASS="<DB_PASS>"
    ./deploy.sh
    ```
3.  脚本运行期间会输出构建和传输进度，请耐心等待直到出现 `Deployment complete.`。

## 验证与排错

*   **后端日志**: 部署完成后，后端进程在后台运行，标准输出和错误流被重定向到 `/opt/wmdb/backend/backend.log`。您可以登录服务器查看该日志以排查启动问题：
    ```bash
    ssh <SERVER_USER>@<SERVER_IP>
    # 输入密码
    tail -f /opt/wmdb/backend/backend.log
    ```
*   **前端访问**: 部署完成后，可通过配置的 Nginx 或直接访问服务地址进行验证 (注：本手册的脚本仅放置了前端静态文件到 `/opt/wmdb/frontend`，如需提供 Web 服务，请在服务器上配置 Nginx 代理静态文件及 `/api` 路由)。
